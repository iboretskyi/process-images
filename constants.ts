import configurations from './assets/configurations.json';

export const ROOT_DIRECTORY = './images';

export const IMAGE_EXTENSIONS = ['.jpg', '.jpeg', '.png', '.gif', '.webp', '.tif', '.tiff'];

export const CONFIGURATIONS = configurations;
